- prep-commit-message.hook: cherry-pick should not add ";no conflicts" if --no-commit used
- generate "Available in:" list jira comment (may match on commit message string)
- zbx,box - it can start stopped container if found instead of recreating
- automate todo list to be extracted from scripts and published
- fix changelog generator added excess whitespace in front also no flags on empty
- ensure zabbix agent interface of type DNS is set correctly
- automate to remove worktree dir and db (removing workspace fully)
- automate pg_restore --clean -Upostgres -d zabbix ./zbx-16162.backup
