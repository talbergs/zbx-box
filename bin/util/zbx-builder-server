#!/bin/bash
set -e

./bootstrap.sh

./configure -q \
    --enable-server \
    --enable-agent \
    --with-libcurl \
    --enable-proxy \
    --with-postgresql \
    --with-mysql \
    --prefix="$PWD/dist"

make
make install
